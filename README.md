1. `cd merrimack-museum/`
2. `npm install`.
3. Create `merrimack-museum/.env.local` and add the required information.
4. Return to the project root and run `bash scripts/import_database.sh`
5. `cd merrimack-museum/`

- `npm run dev`
- `npm run dev:preview` - can show admin/faculty pages
- `npm run dev:real` -uses real login
